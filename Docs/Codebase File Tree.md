# Right-Click Menu API — File Tree

## Root

```
WORKING/
├── README.md              94 lines   Top-level orientation + iteration plan
├── Rough Spec.md         ~600 lines  Long-form design + risk analysis
├── CMakeLists.txt         16 lines   Alternative build (test_harness only — incomplete)
├── build.bat              62 lines   Primary build script (cl.exe direct)
├── register.bat           30 lines   regsvr32 /s build\shell_extension.dll
├── unregister.bat         20 lines   regsvr32 /s /u build\shell_extension.dll
├── codebase_analysis.md  ~580 lines  Generated by /document
├── Docs/                  Documentation (this folder)
│   ├── Quick Guide.md            ~470 lines  Hub doc — generated by /document
│   ├── Codebase File Tree.md     this file   Generated by /document
│   └── Deployment Status.md       ~75 lines  Hand-written deployment notes
├── Font/                  Exo 2 typeface (Google Fonts, OFL)
│   ├── Exo2-VariableFont_wght.ttf            295 KB  THE file loaded at runtime
│   ├── Exo2-Italic-VariableFont_wght.ttf     294 KB  unused
│   ├── OFL.txt, README.txt                            license + readme
│   └── static/                                        18 static-weight TTFs (unused at runtime)
├── assets/
│   └── icons/             9 PNGs, 28×28, transparent backgrounds
│       view.png, sort_by.png, group_by.png,
│       copy.png, cut.png, paste.png,
│       delete.png, rename.png, properties.png
├── src/                   Host shell-extension DLL source (19 files)
├── examples/menu.cpp      Sample consumer — Win11-mockup menu
├── test_harness/          Stage-1 standalone test exe
├── hook_test/             Stage-2 standalone test exe
├── test_scripts/          Python helpers (extract_icons, print_font_names)
└── build/                 Generated cl.exe outputs
```


## `src/` — host DLL source

```
src/
├── menu_api.h              ~165 lines  Consumer-facing API surface. Single include
│                                       for consumers. Macros (Selection / Submenu /
│                                       Separator), context globals, predicates,
│                                       MENU_API_DECL tri-mode export attribute.
├── menu_api.cpp             ~80 lines  Macro helpers + operator+ overloads + tree
│                                       builder + handler registry storage.
├── menu_node.h              ~37 lines  MenuNode tree-node type + handler registry
│                                       decls. Predicates is_separator/selection/submenu.
│
├── menu_window.h            20 lines   menu_window::show entry point.
├── menu_window.cpp        ~1100 lines  Owner-drawn WS_EX_LAYERED popup.
│                                       Three-pass paint: GDI+ (PARGB) for silhouette
│                                       + highlights + icons + separators; D2D +
│                                       DirectWrite (private WIC bitmap, composited
│                                       back) for text; GDI+ stroke for navy border.
│                                       DPI scaling via S(v) / Sf(v) helpers.
│                                       Owns: Exo 2 font collection (DirectWrite
│                                       FontSetBuilder), GDI+ startup token,
│                                       icon cache (Gdiplus::Bitmap* per path).
│
├── context.h                30 lines   Host-side context helpers (setters).
├── context.cpp            ~120 lines   Context globals storage + extension parsing
│                                       + predicates (extension_exists, path_matches
│                                       glob with * and ?).
├── shell_context.h          20 lines   populate_shell_context() public entry.
├── shell_context.cpp      ~250 lines   IShellWindows → IShellBrowser → IShellView →
│                                       IFolderView → IShellItemArray pipeline.
│                                       Infers ClickTarget from selection paths.
│                                       Falls back to Desktop on Progman/WorkerW class.
│
├── hook.h                   71 lines   IATHook class + bulk install/uninstall decls.
├── hook.cpp               ~520 lines   IATHook impl (PE walker + VirtualProtect).
│                                       Inline patch impl (12-byte mov rax;jmp rax).
│                                       TrackPopupMenu(Ex)_detour. install_iat_hooks
│                                       enumerates loaded modules + patches each.
│                                       install_loadlibrary_hook for runtime module
│                                       additions.
│
├── detours_hook.h           12 lines   install/uninstall_cocreateinstance_hook decls.
├── detours_hook.cpp       ~145 lines   IAT-hooks ole32!CoCreateInstance and
│                                       combase!CoCreateInstance. Detour refuses
│                                       CLSIDs in kRefusedClsids[] (1 placeholder).
│                                       NO Microsoft Detours dependency — uses our
│                                       own IATHook.
│
├── class_factory.h          25 lines   CLSID + CreateRightClickMenuClassFactory
│                                       + rcm_global_object_count decls.
├── class_factory.cpp      ~150 lines   RcmContextMenu (IShellExtInit + IContextMenu
│                                       stub returning 0 items added) +
│                                       RcmClassFactory (standard IClassFactory).
│                                       Lifetime via g_object_count.
│
├── main.cpp                323 lines   DllMain (process attach: load consumer DLL,
│                                       install all hooks, all SEH-wrapped).
│                                       build_menu_for_current_click. COM exports:
│                                       DllGetClassObject, DllCanUnloadNow,
│                                       DllRegisterServer, DllUnregisterServer
│                                       (writes HKCR\CLSID + per-asset
│                                       ContextMenuHandlers + HKLM Approved).
│
├── shell_extension.def       7 lines   EXPORTS for the four DllX COM functions.
│
├── diag.h                   14 lines   Logger interface.
└── diag.cpp                 50 lines   File logger impl. Single mutex, append,
                                        timestamped [HH:MM:SS.mmm].
```


## `examples/`

```
examples/
└── menu.cpp                ~100 lines  Sample consumer — Win11-mockup menu (12 items).
                                        View / Sort by / Group by submenus →
                                        separator → Copy / Cut / Paste (Ctrl shortcuts)
                                        → separator → Delete (Del) / Rename (F2) →
                                        separator → Properties. Each item has
                                        (text, shortcut, icon). Handlers SendInput
                                        the corresponding key combo.
                                        Compiles to build/menu.dll, links
                                        build/shell_extension.lib.
```


## `test_harness/`

```
test_harness/
└── test_harness.cpp        119 lines   Stage-1 test exe. Static-link the API
                                        (MENU_API_STATIC), no hooks. Hardcodes
                                        context (paths/target), small RegisterMenu
                                        with 3 selections + nested submenu, calls
                                        menu_window::show at cursor, dispatches
                                        clicked handler.
```


## `hook_test/`

```
hook_test/
└── hook_test.cpp            65 lines   Stage-2 test exe. LoadLibrary the host DLL
                                        (which installs hooks in this process), then
                                        call TrackPopupMenuEx directly. Validates
                                        hook pipeline without explorer.exe. Logs
                                        patched-slot value before/after.
```


## `test_scripts/`

```
test_scripts/
├── extract_icons.py       ~135 lines   PIL + numpy. Opens ChatGPT 3×3 icon grid
│                                       PNG, finds 9 colored rounded-square blobs
│                                       via saturation mask + connected-components,
│                                       sorts row-major, crops + resizes to 28×28
│                                       with transparent backgrounds, writes to
│                                       WORKING/assets/icons/.
└── print_font_names.py     ~35 lines   Parses TTF name table to print
                                        Family/Subfamily/Full/PostScript names.
                                        Used to verify variable font's family
                                        is recognised as "Exo 2" by DirectWrite.
```


## `Docs/`

```
Docs/
├── Quick Guide.md          ~470 lines  Hub document. Project meta, architectural
│                                       invariants, key paths/constants/funcs/data,
│                                       error handling, dispatch flow, setup,
│                                       recipes, Pending/WIP. Regenerated by
│                                       /document.
├── Codebase File Tree.md     this file Annotated tree + ASCII diagrams.
│                                       Regenerated by /document.
└── Deployment Status.md     ~75 lines  Hand-written deployment notes from the
                                        live-explorer integration session.
                                        NOT regenerated.
```


## Counts

| Metric                        | Value                       |
|-------------------------------|----------------------------:|
| Total source-code files       | 22                          |
| C++ source/header files       | 19 in `src/` + 3 in test/example dirs |
| Python helper scripts         | 2                           |
| Documentation files           | 3 in `Docs/` + 2 root + analysis = 6 total |
| Build / config files          | 4 (build.bat, register.bat, unregister.bat, CMakeLists.txt) + 1 .def |
| Asset files                   | 9 PNG icons + 20 TTF fonts (1 used)  |
| Generated build artifacts     | 4 final (.dll, .dll, .exe, .exe) + .obj/.lib/.exp/.log/.exp |
| External library dependencies | 0 (only Win32/COM/GDI+/D2D/DirectWrite/WIC, all in SDK) |
| Total source lines (approx)   | ~3,300 |


## Persisted paths

| Path                                                              | Written by                  | Format                                                            |
|-------------------------------------------------------------------|-----------------------------|-------------------------------------------------------------------|
| `build/shell_extension.log`                                       | `diag::log` (`diag.cpp`)    | Append, mutex, timestamped `[HH:MM:SS.mmm]` + format-string + `\n` |
| `build/*.dll`, `build/*.exe`                                      | `cl.exe` via `build.bat`    | Standard Windows PE                                               |
| `build/obj/*.obj`                                                 | `cl.exe`                    | MSVC object files                                                 |
| `build/shell_extension.lib`, `build/shell_extension.exp`          | `cl.exe /LD /DEF:...`       | Import library + export file (consumed by menu.dll's link)        |
| `build/Font/Exo2-VariableFont_wght.ttf`                           | `robocopy` in `build.bat`   | Mirrored from `Font/`                                             |
| `build/assets/icons/*.png`                                        | `robocopy` in `build.bat`   | Mirrored from `assets/icons/`                                     |
| `HKCR\CLSID\{7C3F4C5E-9D2E-4F7A-B5C8-1A3D4E5F6A7B}`               | `DllRegisterServer`         | REG_SZ "Right-Click Menu API"                                     |
| `HKCR\CLSID\{...}\InProcServer32`                                 | `DllRegisterServer`         | REG_SZ DLL path + ThreadingModel = "Apartment"                    |
| `HKCR\<asset>\shellex\ContextMenuHandlers\RightClickMenuAPI`      | `DllRegisterServer`         | REG_SZ "{guid}" — for asset = `*`, `AllFilesystemObjects`, `Directory`, `Directory\Background`, `Drive`, `Folder` |
| `HKLM\Software\Microsoft\Windows\CurrentVersion\Shell Extensions\Approved` | `DllRegisterServer` | Named value = "{guid}", data = "Right-Click Menu API"          |


## Control-Flow Diagrams

### System high-level

```
            ┌──────────────────────────────────────────────┐
            │  CONSUMER  (examples/menu.cpp → menu.dll)    │
            │                                              │
            │  void RegisterMenu() {                       │
            │      Submenu("View", "view.png") {           │
            │          Selection("Large icons") { ... };   │
            │      };                                      │
            │      Separator();                            │
            │      Selection("Copy", "Ctrl+C", "copy.png") │
            │          { SendKeys('C', /*ctrl=*/true); };  │
            │  }                                           │
            └─────────────────────┬────────────────────────┘
                                  │ exports RegisterMenu (dllexport)
                                  │ imports menu_api symbols (dllimport)
                                  ▼
   ┌─────────────────────────────────────────────────────────────┐
   │  HOST  (src/* → shell_extension.dll)                        │
   │                                                              │
   │   ┌─────────┐    ┌──────────────┐    ┌──────────────────┐   │
   │   │ DllMain │───▶│ install      │───▶│ user32 patched   │   │
   │   │         │    │   IAT hooks  │    │ TrackPopupMenuEx │   │
   │   │         │    │   + inline   │    │   redirects to   │   │
   │   │         │    │    patch     │    │   our detour     │   │
   │   └─────────┘    └──────────────┘    └────────┬─────────┘   │
   │                                                │             │
   │                                                ▼             │
   │   ┌──────────────────────────────────────────────────────┐   │
   │   │  prime_context (shell_context.cpp)                   │   │
   │   │     IShellWindows → IShellBrowser → IShellView       │   │
   │   │     → IFolderView → IShellItemArray (selection)      │   │
   │   │     → set_paths / set_target / set_modifiers         │   │
   │   │                                                       │   │
   │   │  build_menu_for_current_click (main.cpp)             │   │
   │   │     reset_menu_state + g_registerMenu()              │   │
   │   │     (consumer's RegisterMenu populates rootMenu)     │   │
   │   │                                                       │   │
   │   │  menu_window::show (menu_window.cpp)                 │   │
   │   │     paint_layered:                                   │   │
   │   │     ┌──────────────┐ ┌──────────────┐ ┌────────────┐ │   │
   │   │     │ GDI+ on PARGB│ │ D2D + DWrite │ │ GDI+ stroke│ │   │
   │   │     │ silhouette + │ │ via private  │ │ navy border│ │   │
   │   │     │ icons + sep  │ │ WIC bitmap   │ │ same path  │ │   │
   │   │     └──────┬───────┘ └──────┬───────┘ └─────┬──────┘ │   │
   │   │            └────────────────┼────────────────┘        │   │
   │   │                             ▼                         │   │
   │   │                    UpdateLayeredWindow(ULW_ALPHA)     │   │
   │   │                                                       │   │
   │   │  dispatch_handler                                     │   │
   │   │     handlerRegistry[id]()  (consumer's lambda)        │   │
   │   └──────────────────────────────────────────────────────┘   │
   └───────────────────────────────────────────────────────────────┘
                                  ▲
                                  │ loaded via InProcServer32
            ┌─────────────────────┴────────────────────────┐
            │  HOST PROCESS  (explorer.exe / hook_test.exe │
            │   / test_harness.exe)                        │
            └──────────────────────────────────────────────┘
```


### Per-click dispatch zoom

```
TrackPopupMenuEx(HMENU, flags, x, y, hwnd, params)
   │
   │  ── intercepted by 12-byte inline patch at function entry ──
   │     [mov rax, &TrackPopupMenuEx_detour]
   │     [jmp rax]
   │
   ▼
TrackPopupMenuEx_detour          [hook.cpp]
   │
   │  __try {
   │
   ├──▶ prime_context(owner, x, y)         [hook.cpp]
   │     │
   │     ├─ populate_shell_context()       [shell_context.cpp]
   │     │   ├─ CoInitializeEx (handle already-init)
   │     │   ├─ CoCreateInstance(CLSID_ShellWindows, CLSCTX_ALL)
   │     │   ├─ Iterate IShellWindows::Item(i) → IDispatch
   │     │   │  → IServiceProvider::QueryService(SID_STopLevelBrowser)
   │     │   │  → IShellBrowser
   │     │   ├─ Match IShellBrowser::GetWindow(&hw) == owner_top
   │     │   ├─ QueryActiveShellView → IShellView → QI IFolderView
   │     │   ├─ Items(SVGIO_SELECTION, IShellItemArray) → enumerate paths
   │     │   ├─ QI IShellFolder → IPersistFolder2::GetCurFolder
   │     │   │  → SHGetPathFromIDListW (folder path → parentFolder)
   │     │   └─ infer_single_target() — Drive / Folder / File / VirtualItem
   │     ├─ set_target(...) / set_paths(...) / parentFolder = ...
   │     └─ set_click_location(x, y) + set_modifiers (GetKeyState)
   │
   ├──▶ build_menu_for_current_click       [main.cpp]
   │     │  __try {
   │     ├─ reset_menu_state                [menu_api.cpp]
   │     │   ├─ rootMenu = MenuNode{}
   │     │   ├─ currentMenu = &rootMenu
   │     │   └─ handlerRegistry.clear()
   │     │
   │     └─ g_registerMenu()                ← consumer's exported RegisterMenu
   │          │
   │          ├─ Selection("X", "Sc", "I.png") { body };
   │          │   expands to:
   │          │     selection_helper("X", "Sc", "I.png") + [&]{ body };
   │          │       ├─ returns SelectionTag{"X", "Sc", "I.png"}
   │          │       └─ operator+(SelectionTag, std::function<void()>):
   │          │            [menu_api.cpp]
   │          │            ├─ handlerId = handlerRegistry.size()
   │          │            ├─ handlerRegistry.push_back(lambda)
   │          │            └─ currentMenu->children.push_back(leaf)
   │          │
   │          ├─ Submenu("Y", "I.png") { ... };
   │          │   expands to:
   │          │     submenu_helper("Y", "I.png") + [&]{ ... };
   │          │       ├─ returns SubmenuTag{"Y", "I.png"}
   │          │       └─ operator+(SubmenuTag, std::function<void()>):
   │          │            ├─ push new submenu node into currentMenu->children
   │          │            ├─ saved = currentMenu; currentMenu = &new
   │          │            ├─ body()    ← inner Selections register HERE
   │          │            └─ currentMenu = saved
   │          │
   │          └─ Separator();
   │              expands to push_separator():
   │                ├─ MenuNode sep; sep.is_sep = true;
   │                └─ currentMenu->children.push_back(sep)
   │
   ├──▶ rootMenu.children.empty() ?
   │      yes → return FALSE  (no menu to show)
   │      no  → continue
   │
   ├──▶ menu_window::show(rootMenu, x, y, hwnd)
   │     │  [menu_window.cpp → show_impl]
   │     │
   │     ├─ Set g_dpi_scale (= 1.0 currently)
   │     ├─ compute_width + total height = N * S(kItemHeight) + 2*S(kFramePadV)
   │     ├─ CreateWindowExW (WS_EX_LAYERED | WS_EX_TOPMOST | WS_EX_TOOLWINDOW)
   │     │   No DwmSetWindowAttribute (would draw NC frame)
   │     │   No CS_DROPSHADOW (would draw rectangular outline)
   │     ├─ paint_layered:
   │     │   ┌─ Allocate 32bpp BI_RGB top-down DIB at W×H
   │     │   ├─ memset(bits, 0, W*H*4)  — all-transparent baseline
   │     │   │
   │     │   ├─ PASS 1: paint_contents_gdi(W, H, bits, d)
   │     │   │   Gdiplus::Bitmap PARGB on bits, Graphics::FromImage
   │     │   │   ├─ FillPath(rounded silhouette, kBg=white)
   │     │   │   └─ for each child:
   │     │   │       ├─ separator → DrawLine(navy, 1.5px)
   │     │   │       ├─ highlighted → FillPath(item rounded rect, kHighlight)
   │     │   │       └─ DrawImage(IconBitmap.gpb, target rect, HQ bicubic)
   │     │   │
   │     │   ├─ PASS 2: paint_contents_text(W, H, bits, d)
   │     │   │   CoInitializeEx STA, IWICImagingFactory
   │     │   │   ├─ IWICBitmap::CreateBitmap(W, H, PBGRA, CacheOnLoad)
   │     │   │   ├─ Lock + memset to 0 (transparent)
   │     │   │   ├─ ID2D1RenderTarget on WIC bitmap (PARGB, DPI 96)
   │     │   │   ├─ BeginDraw, SetTextAntialiasMode(GRAYSCALE)
   │     │   │   ├─ for each non-separator child:
   │     │   │   │   ├─ label   DrawText (Exo 2, wght=450, kText)
   │     │   │   │   ├─ chevron DrawText (Segoe UI Symbol, U+2B9E, kBorder)
   │     │   │   │   └─ shortcut DrawText (Exo 2, wght=250, kShortcut)
   │     │   │   ├─ Flush, EndDraw
   │     │   │   └─ Lock(WICBitmapLockRead) + manual src-over composite
   │     │   │       onto bits[] (premultiplied alpha)
   │     │   │
   │     │   ├─ PASS 3: GDI+ DrawPath border
   │     │   │   ├─ Same path geometry as silhouette (rounded rect)
   │     │   │   ├─ Pen alignment Center, navy, 1.0px
   │     │   │   ├─ Path inset by pw/2, corner radius reduced by pw/2
   │     │   │   └─ AA aligns with silhouette
   │     │   │
   │     │   └─ UpdateLayeredWindow(hwnd, screen, dstPt, sz, memDC,
   │     │                          srcPt, 0,
   │     │                          BLENDFUNCTION{AC_SRC_OVER, ULW_ALPHA},
   │     │                          ULW_ALPHA)
   │     │
   │     ├─ ShowWindow(SW_SHOWNOACTIVATE)
   │     ├─ SetForegroundWindow + SetCapture + SetCursor(IDC_ARROW)
   │     │
   │     └─ modal loop: while (!data.done) GetMessage / DispatchMessage
   │           │
   │           ├─ WM_PAINT → paint_layered (re-render on damage)
   │           ├─ WM_MOUSEMOVE → SetCursor(IDC_ARROW); hit-test
   │           │     ├─ if hi >= 0 && submenu → show_impl(child) recurse
   │           │     │     (parent's WndProc on stack while child runs)
   │           │     │
   │           │     └─ if hi < 0 && cursor over owner on different item:
   │           │           auto-dismiss self
   │           │
   │           ├─ WM_LBUTTONUP → if on Selection: result = handlerId; done = true
   │           ├─ WM_KEYDOWN(ESC) → done = true
   │           └─ WM_SETCURSOR → force IDC_ARROW (defeat wait cursor)
   │
   ├──▶ dispatch_handler(clicked_id)        [hook.cpp]
   │     │  __try {
   │     └─ handlerRegistry[clicked_id]()    ← consumer's lambda body runs
   │     │  } __except { swallow }
   │
   ├──▶ return TRUE
   │
   │  } __except (EXCEPTION_EXECUTE_HANDLER) {
   │       return FALSE;   ← any fault in our code falls back; host stays alive
   │  }
   ▼
(returns to caller of TrackPopupMenuEx — explorer or hook_test)
```
